services:
  - type: web
    name: churn-prediction-api
    runtime: docker
    rootDir: model
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
